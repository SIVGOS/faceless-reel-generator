# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# DB
SQLAlchemy==2.0.36

# Auth
# passlib 1.7.4 reads bcrypt.__about__, removed in bcrypt 4.1+. Pin to 4.0.x.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT==2.10.1

# Config
pydantic==2.10.4
pydantic-settings==2.7.1

# AI text + native Gemini TTS. The TTS speech-generation API (response_modalities
# AUDIO + SpeechConfig) needs the 2.x SDK; 0.3.0 predates it.
google-genai==2.5.0

# Media pipeline
# edge-tts must track Microsoft's rotating Sec-MS-GEC token; older pins 403.
edge-tts==7.2.8
faster-whisper==1.1.0
# Animated captions (v2 caption engine). 2.x API: with_*/resized/cropped, PIL
# TextClip (no ImageMagick). Pulls pillow + imageio-ffmpeg.
moviepy==2.1.2

# Tests
pytest==8.3.4
httpx==0.28.1
